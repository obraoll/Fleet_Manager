<Window x:Class="FleetManager.Views.SendReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Envoi de Rapport par Email" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButton}"/>
            <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButton}"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0" Text="📧 Envoi de Rapport par Email" 
                   FontSize="20" FontWeight="Bold" 
                   Foreground="#1976D2" Margin="0,0,0,20"/>

        <!-- Formulaire -->
        <Border Grid.Row="1" Style="{StaticResource ModernCard}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Destinataire -->
                    <TextBlock Text="Destinataire *" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="RecipientTextBox" 
                             Text="{Binding RecipientEmail, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Adresse email du destinataire"/>

                    <!-- Liste de destinataires suggérés -->
                    <TextBlock Text="Destinataires suggérés" Style="{StaticResource LabelStyle}"/>
                    <ListBox ItemsSource="{Binding SuggestedEmails}" 
                             SelectionMode="Single"
                             Height="100"
                             SelectionChanged="SuggestedEmail_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📧" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Type de rapport -->
                    <TextBlock Text="Type de rapport *" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="ReportTypeComboBox" 
                              SelectedItem="{Binding SelectedReportType}"
                              Height="35">
                        <ComboBoxItem Content="📊 Statistiques Générales" IsSelected="True"/>
                        <ComboBoxItem Content="📈 Rapport Mensuel"/>
                        <ComboBoxItem Content="💰 Rapport de Coûts"/>
                        <ComboBoxItem Content="⛽ Rapport de Consommation"/>
                        <ComboBoxItem Content="🔧 Rapport de Maintenance"/>
                    </ComboBox>

                    <!-- Période -->
                    <TextBlock Text="Période" Style="{StaticResource LabelStyle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <DatePicker Grid.Column="0" 
                                    SelectedDate="{Binding StartDate}"
                                    FirstDayOfWeek="Monday"
                                    Height="35"/>
                        <TextBlock Grid.Column="1" Text=" à " 
                                   VerticalAlignment="Center" 
                                   Margin="10,0"/>
                        <DatePicker Grid.Column="2" 
                                    SelectedDate="{Binding EndDate}"
                                    FirstDayOfWeek="Monday"
                                    Height="35"/>
                    </Grid>

                    <!-- Message personnalisé -->
                    <TextBlock Text="Message (optionnel)" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding CustomMessage, UpdateSourceTrigger=PropertyChanged}"
                             Height="80"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"/>

                    <!-- Format -->
                    <TextBlock Text="Format du fichier" Style="{StaticResource LabelStyle}"/>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="📄 PDF" 
                                     IsChecked="{Binding IsPdfFormat}" 
                                     Margin="0,0,20,0" 
                                     VerticalAlignment="Center"/>
                        <RadioButton Content="📊 Excel" 
                                     IsChecked="{Binding IsExcelFormat}" 
                                     VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Options -->
                    <TextBlock Text="Options" Style="{StaticResource LabelStyle}"/>
                    <StackPanel>
                        <CheckBox Content="Inclure les graphiques" 
                                  IsChecked="{Binding IncludeCharts}" 
                                  Margin="0,5"/>
                        <CheckBox Content="Inclure les détails par véhicule" 
                                  IsChecked="{Binding IncludeVehicleDetails}" 
                                  Margin="0,5"/>
                        <CheckBox Content="Inclure les recommandations" 
                                  IsChecked="{Binding IncludeRecommendations}" 
                                  Margin="0,5"/>
                    </StackPanel>

                    <!-- État d'envoi -->
                    <Border Background="#E3F2FD" 
                            CornerRadius="5" 
                            Padding="10" 
                            Margin="0,15,0,0"
                            Visibility="{Binding StatusMessageVisibility}">
                        <TextBlock Text="{Binding StatusMessage}" 
                                   TextWrapping="Wrap" 
                                   Foreground="#1565C0"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Boutons -->
        <Border Grid.Row="2" Background="White" Padding="15" Margin="-20,10,-20,-20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="📧 Envoyer" Width="150" Height="40"
                        Command="{Binding SendCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,10,0"/>
                <Button Content="Annuler" Width="120" Height="40"
                        IsCancel="True"
                        Click="CancelButton_Click"
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
