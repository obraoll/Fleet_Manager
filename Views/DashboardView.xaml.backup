<!-- Views/DashboardView.xaml -->
<UserControl x:Class="FleetManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FleetManager.Views"
             xmlns:viewmodels="clr-namespace:FleetManager.ViewModels"
             xmlns:components="clr-namespace:FleetManager.Components"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F8FAFC">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
        <StackPanel>
            
            <!-- ======================================================================================== -->
            <!-- HEADER MODERNE AVEC GRADIENT -->
            <!-- ======================================================================================== -->
            <Grid Margin="0,0,0,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Titre et sous-titre -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Tableau de bord"
                               FontFamily="{StaticResource PrimaryFont}"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="#1E293B"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Border Background="#EEF2FF" 
                                CornerRadius="6" 
                                Padding="10,6">
                            <TextBlock Text="{Binding LastUpdated, StringFormat='Dernière mise à jour : {0:HH:mm}'}"
                                       FontSize="13"
                                       Foreground="#6366F1"/>
                        </Border>
                        
                        <Border Background="#DCFCE7" 
                                CornerRadius="6" 
                                Padding="10,6"
                                Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="●" 
                                           Foreground="#16A34A" 
                                           FontSize="14" 
                                           Margin="0,0,6,0"/>
                                <TextBlock Text="Système opérationnel"
                                           FontSize="13"
                                           Foreground="#16A34A"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <!-- Boutons d'action -->
                <StackPanel Grid.Column="1" 
                           Orientation="Horizontal"
                           VerticalAlignment="Center">
                    <Button Height="44" 
                            Padding="16,0"
                            Margin="0,0,12,0"
                            Background="White"
                            BorderBrush="#E2E8F0"
                            BorderThickness="1"
                            Cursor="Hand"
                            Command="{Binding RefreshCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="10"
                                                    Padding="{TemplateBinding Padding}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🔄" FontSize="16" Margin="0,0,8,0"/>
                                                    <TextBlock Text="Actualiser" 
                                                              FontSize="14" 
                                                              Foreground="#475569"
                                                              VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F8FAFC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Height="44" 
                            Padding="20,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#667eea" Offset="0"/>
                                            <GradientStop Color="#764ba2" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="10"
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="#667eea" 
                                                                     Direction="0" 
                                                                     ShadowDepth="0" 
                                                                     Opacity="0.3" 
                                                                     BlurRadius="15"/>
                                                </Border.Effect>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0"/>
                                                    <TextBlock Text="Rapport" 
                                                              FontSize="14" 
                                                              FontWeight="SemiBold"
                                                              VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- ======================================================================================== -->
            <!-- ALERTES CRITIQUES (SI PRÉSENTES) -->
            <!-- ======================================================================================== -->
            <Border Background="#FEF2F2"
                    BorderBrush="#FCA5A5"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="20"
                    Margin="0,0,0,32"
                    Visibility="{Binding CriticalAlerts, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="40" Height="40" 
                                Background="#DC2626" 
                                CornerRadius="10"
                                Margin="0,0,16,0">
                            <TextBlock Text="⚠️" 
                                      FontSize="20" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Alertes critiques"
                                      FontSize="18"
                                      FontWeight="Bold"
                                      Foreground="#DC2626"/>
                            <TextBlock Text="Attention requise immédiatement"
                                      FontSize="13"
                                      Foreground="#7F1D1D"
                                      Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding Alerts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White"
                                        BorderBrush="#FCA5A5"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" 
                                                  Text="🚨" 
                                                  FontSize="18" 
                                                  Margin="0,0,12,0"
                                                  VerticalAlignment="Center"/>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding VehicleRegistration}"
                                                      FontSize="14"
                                                      FontWeight="SemiBold"
                                                      Foreground="#1E293B"/>
                                            <TextBlock Text="{Binding Message}"
                                                      FontSize="13"
                                                      Foreground="#64748B"
                                                      Margin="0,4,0,0"/>
                                        </StackPanel>

                                        <Button Grid.Column="2"
                                               Content="Traiter"
                                               Background="#DC2626"
                                               Foreground="White"
                                               BorderThickness="0"
                                               Padding="12,6"
                                               FontSize="12"
                                               Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="6"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                                    VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.9"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- ======================================================================================== -->
            <!-- CARTES KPI MODERNES -->
            <!-- ======================================================================================== --> 
                                  Foreground="{StaticResource DangerBrush}"/>
                        <ItemsControl ItemsSource="{Binding Alerts}" MaxHeight="150" Margin="0,16,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource BadgeDanger}"
                                            Margin="0,0,0,8"
                                            HorizontalAlignment="Stretch"
                                            Padding="12">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🚨" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding VehicleRegistration}" 
                                                      FontWeight="Bold" 
                                                      Margin="0,0,8,0"/>
                                            <TextBlock Text=" - " Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Message}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- ============================================================================ -->
                <!-- INDICATEURS CLÉS (KPI) - DESIGN MODERNE -->
                <!-- ============================================================================ -->
                <TextBlock Style="{StaticResource H2}" 
                          Text="📈 Indicateurs clés" 
                          Margin="0,0,0,24"/>

                <Grid Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Véhicules -->
                    <components:StatsCard Grid.Column="0" 
                                         Icon="🚗"
                                         IconBackground="{StaticResource PrimaryBrush}"
                                         Value="{Binding TotalVehicles}"
                                         Label="Véhicules totaux"
                                         TrendText="{Binding ActiveVehiclePercentage, StringFormat={}{0:F0}% actifs}"
                                         TrendIcon="✓"
                                         TrendColor="{StaticResource SuccessBrush}"
                                         TrendVisibility="Visible"
                                         Margin="0,0,16,0"/>

                    <!-- Total Pleins -->
                    <components:StatsCard Grid.Column="1" 
                                         Icon="⛽"
                                         IconBackground="{StaticResource SuccessBrush}"
                                         Value="{Binding TotalFuelRecords}"
                                         Label="Pleins enregistrés"
                                         TrendText="{Binding MonthlyFuelCost, StringFormat={}{0:C} ce mois}"
                                         TrendIcon="↑"
                                         TrendColor="{StaticResource WarningBrush}"
                                         TrendVisibility="Visible"
                                         Margin="0,0,16,0"/>

                    <!-- Consommation Moyenne -->
                    <components:StatsCard Grid.Column="2" 
                                         Icon="📊"
                                         IconBackground="{StaticResource WarningBrush}"
                                         Value="{Binding AverageFuelConsumption, StringFormat={}{0:F1}}"
                                         Label="L/100km (moyenne)"
                                         TrendText="Moyenne flotte"
                                         TrendIcon="="
                                         TrendColor="{StaticResource InfoBrush}"
                                         TrendVisibility="Visible"
                                         Margin="0,0,16,0"/>

                    <!-- Coût Carburant -->
                    <components:StatsCard Grid.Column="3" 
                                         Icon="💰"
                                         IconBackground="{StaticResource DangerBrush}"
                                         Value="{Binding TotalFuelCost, StringFormat={}{0:C}}"
                                         Label="Coût carburant"
                                         TrendText="{Binding FuelToMaintenanceRatio, StringFormat=Ratio: {0:F1}}"
                                         TrendIcon="📈"
                                         TrendColor="{StaticResource TextSecondaryBrush}"
                                         TrendVisibility="Visible"
                                         Margin="0,0,16,0"/>

                    <!-- Maintenance -->
                    <components:StatsCard Grid.Column="4" 
                                         Icon="🔧"
                                         IconBackground="{StaticResource InfoBrush}"
                                         Value="{Binding TotalMaintenanceCost, StringFormat={}{0:C}}"
                                         Label="Maintenance"
                                         TrendText="{Binding VehiclesInMaintenance, StringFormat={}{0} en cours}"
                                         TrendIcon="⚙️"
                                         TrendColor="{StaticResource WarningBrush}"
                                         TrendVisibility="Visible"/>
                </Grid>

                <!-- ============================================================================ -->
                <!-- GRAPHIQUES ET ANALYSES -->
                <!-- ============================================================================ -->
                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Graphiques des tendances -->
                    <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Style="{StaticResource H2}" Text="📈 Tendances sur 30 jours"/>

                            <TabControl Background="Transparent" BorderThickness="0">
                                <TabItem Header="Consommation" Background="White">
                                    <Grid Height="200" Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="28"/>
                                        </Grid.RowDefinitions>
                                        <lvc:CartesianChart x:Name="DashboardConsumptionChart" Series="{Binding ConsumptionSeries}" Grid.Row="0" LegendPosition="Top" />
                                        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                            <ItemsControl ItemsSource="{Binding TrendLabels}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding}" FontSize="10" Margin="6,0" HorizontalAlignment="Center">
                                                            <TextBlock.RenderTransform>
                                                                <RotateTransform Angle="45"/>
                                                            </TextBlock.RenderTransform>
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </TabItem>

                                <TabItem Header="Coûts" Background="White">
                                    <Grid Height="200" Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="28"/>
                                        </Grid.RowDefinitions>
                                        <lvc:CartesianChart x:Name="DashboardCostChart" Series="{Binding CostSeries}" Grid.Row="0" LegendPosition="Top" />
                                        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                            <ItemsControl ItemsSource="{Binding TrendLabels}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding}" FontSize="10" Margin="6,0" HorizontalAlignment="Center">
                                                            <TextBlock.RenderTransform>
                                                                <RotateTransform Angle="45"/>
                                                            </TextBlock.RenderTransform>
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </TabItem>
                            </TabControl>
                        </StackPanel>
                    </Border>

                    <!-- Répartition par type -->
                    <Border Grid.Column="1" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource H2}" Text="🚙 Répartition par type"/>

                            <ItemsControl ItemsSource="{Binding VehicleTypeStats}" MaxHeight="200">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                                                CornerRadius="4" Padding="8" Margin="2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding TypeName}" FontWeight="Bold" FontSize="12"/>
                                                    <TextBlock Text="{Binding AverageConsumption, StringFormat={}{0:F1} L/100km}"
                                                               FontSize="10" Foreground="#666666"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding Count}" FontWeight="Bold" FontSize="14"/>
                                                    <TextBlock Text="{Binding TotalCost, StringFormat={}{0:C}}"
                                                               FontSize="10" Foreground="#FF9800"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Top véhicules et alertes -->
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Top consommateurs -->
                    <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource H2}" Text="⚡ Top consommateurs"/>

                            <ListView ItemsSource="{Binding TopVehiclesByConsumption}"
                                      Background="Transparent" BorderThickness="0" MaxHeight="200">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding RegistrationNumber}" FontWeight="Bold" FontSize="12"/>
                                                <TextBlock Text="{Binding VehicleName}" FontSize="10" Foreground="#666666"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="1" Text="{Binding AverageConsumption, StringFormat={}{0:F1} L/100km}"
                                                       FontWeight="Bold" FontSize="12" Foreground="#F44336"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <!-- Top coûteux -->
                    <Border Grid.Column="1" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource H2}" Text="💸 Top coûteux"/>

                            <ListView ItemsSource="{Binding TopVehiclesByCost}"
                                      Background="Transparent" BorderThickness="0" MaxHeight="200">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding RegistrationNumber}" FontWeight="Bold" FontSize="12"/>
                                                <TextBlock Text="{Binding VehicleName}" FontSize="10" Foreground="#666666"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="1" Text="{Binding TotalCost, StringFormat={}{0:C}}"
                                                       FontWeight="Bold" FontSize="12" Foreground="#FF9800"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <!-- Alertes détaillées -->
                    <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource H2}" Text="🔔 Alertes"/>

                            <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Alerts}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                                <Border Margin="2" Padding="8" CornerRadius="4"
                                                    Background="#FFFFFF">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Type}" FontSize="12" FontWeight="Bold"/>
                                                        <TextBlock Text="{Binding Priority}" FontSize="10"
                                                                   Foreground="#666666" Margin="10,0,0,0"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding VehicleRegistration}" FontWeight="Bold" FontSize="11"/>
                                                    <TextBlock Text="{Binding Message}" FontSize="10"
                                                               TextWrapping="Wrap" Foreground="#444444"/>
                                                    <TextBlock Text="{Binding Date, StringFormat=dd/MM/yyyy}"
                                                               FontSize="9" Foreground="#999999"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Mouvements récents -->
                <Border Style="{StaticResource ModernCard}" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Style="{StaticResource H2}" Text="📋 Mouvements récents"/>

                        <DataGrid ItemsSource="{Binding RecentMovements}"
                                  AutoGenerateColumns="False"
                                  Background="White"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F8F9FA"
                                  MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Véhicule" Binding="{Binding VehicleName}" Width="200"/>
                                <DataGridTextColumn Header="Type" Binding="{Binding MovementType}" Width="100"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy HH:mm}" Width="120"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                                <DataGridTextColumn Header="Coût" Binding="{Binding Cost, StringFormat={}{0:C}}" Width="80"/>
                                <DataGridTextColumn Header="Kilométrage" Binding="{Binding Mileage, StringFormat={}{0:N0} km}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Statistiques mensuelles - Évolution sur 12 mois -->
                <Border Style="{StaticResource ModernCard}" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Style="{StaticResource H2}" Text="📅 Évolution sur 12 mois"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="300"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Graphique avec LiveCharts -->
                            <lvc:CartesianChart x:Name="MonthlyTrendsChart" 
                                                Series="{Binding MonthlyTrendsSeries}"
                                                Grid.Row="0"
                                                LegendPosition="Top"
                                                TooltipPosition="Top">
                            </lvc:CartesianChart>

                            <!-- Tableau récapitulatif des données mensuelles -->
                            <Border Grid.Row="1" 
                                    Background="#F8F9FA" 
                                    CornerRadius="6" 
                                    Padding="15" 
                                    Margin="0,15,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Totaux -->
                                    <StackPanel Grid.Column="0" Margin="5">
                                        <TextBlock Text="💰 Carburant Total" 
                                                   FontSize="11" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#4CAF50"
                                                   Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding YearlyFuelTotal, StringFormat={}{0:C0}}" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   Foreground="#2E7D32"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Margin="5">
                                        <TextBlock Text="🔧 Maintenance Total" 
                                                   FontSize="11" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#FF9800"
                                                   Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding YearlyMaintenanceTotal, StringFormat={}{0:C0}}" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   Foreground="#E65100"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Margin="5">
                                        <TextBlock Text="📊 Coût Total Annuel" 
                                                   FontSize="11" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#2196F3"
                                                   Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding YearlyOperatingCost, StringFormat={}{0:C0}}" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   Foreground="#0D47A1"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="3" Margin="5">
                                        <TextBlock Text="📈 Moyenne Mensuelle" 
                                                   FontSize="11" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#9C27B0"
                                                   Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding MonthlyAverageCost, StringFormat={}{0:C0}}" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   Foreground="#6A1B9A"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Actions rapides -->
                <Border Style="{StaticResource ModernCard}" Margin="0,20,0,20">
                    <StackPanel>
                        <TextBlock Style="{StaticResource H2}" Text="⚡ Actions rapides"/>

                        <WrapPanel HorizontalAlignment="Center" Margin="0,10">
                                <Button Content="📊 Voir statistiques détaillées"
                                    Padding="15,8" Margin="5"
                                    Background="#2196F3" Foreground="White"
                                    BorderThickness="0"
                                    Command="{Binding ViewDetailedStatisticsCommand}"/>
                            <Button Content="📝 Générer rapport"
                                    Padding="15,8" Margin="5"
                                    Background="#4CAF50" Foreground="White"
                                    BorderThickness="0"
                                    Command="{Binding GenerateReportCommand}"/>
                            <Button Content="📤 Exporter données"
                                    Padding="15,8" Margin="5"
                                    Background="#FF9800" Foreground="White"
                                    BorderThickness="0"
                                    Command="{Binding ExportDataCommand}"/>
                            <Button Content="⚙️ Configuration"
                                    Padding="15,8" Margin="5"
                                    Background="#9E9E9E" Foreground="White"
                                    BorderThickness="0"
                                    Command="{Binding OpenSettingsCommand}"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
