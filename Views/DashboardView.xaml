<UserControl x:Class="FleetManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:helpers="clr-namespace:FleetManager.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F8FAFC">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <helpers:VehicleImagePathConverter x:Key="VehicleImagePathConverter"/>
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        
        <!-- Animation pour les cartes -->
        <Storyboard x:Key="CardHoverAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           To="1.02" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           To="1.02" Duration="0:0:0.2"/>
        </Storyboard>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24">
            <!-- EN-TÊTE Dashboard -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Dashboard" 
                              FontFamily="Inter, Segoe UI"
                              FontSize="28" 
                              FontWeight="Bold" 
                              Foreground="#0F172A" 
                              Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Obtenez vos dernières mises à jour pour les 7 derniers jours" 
                                  FontSize="14" 
                                  Foreground="#64748B"/>
                        <TextBlock Text=" • " 
                                  FontSize="14" 
                                  Foreground="#94A3B8"
                                  Margin="4,0,4,0"/>
                        <TextBlock Text="{Binding LastUpdated}" 
                                  FontSize="14" 
                                  Foreground="#3B82F6"
                                  FontWeight="Medium"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Border Background="Transparent"
                           BorderBrush="#E2E8F0"
                           BorderThickness="1"
                           CornerRadius="8"
                           Padding="12,8"
                           Height="40"
                           Margin="0,0,12,0"
                           Cursor="Hand">
                        <Button Background="Transparent"
                               BorderThickness="0"
                               Foreground="#475569"
                               FontSize="14"
                               Padding="0"
                               Content="🔄 Actualiser"
                               Command="{Binding RefreshCommand}"
                               Cursor="Hand"/>
                    </Border>
                    <Border Background="#3B82F6"
                           CornerRadius="8"
                           Padding="12,8"
                           Height="40"
                           Cursor="Hand">
                        <Button Background="Transparent"
                               BorderThickness="0"
                               Foreground="White"
                               FontSize="14"
                               FontWeight="Medium"
                               Padding="0"
                               Content="📊 Statistiques"
                               Command="{Binding ViewDetailedStatisticsCommand}"
                               Cursor="Hand"/>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- ALERTES (si présentes) -->
            <Border Background="#FEF2F2"
                   BorderBrush="#FCA5A5"
                   BorderThickness="1"
                   CornerRadius="12"
                   Padding="16"
                   Margin="0,0,0,24"
                   Visibility="{Binding AlertCount, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="⚠️" FontSize="18" Margin="0,0,12,0"/>
                        <TextBlock Text="Alertes Critiques" 
                                  FontSize="16" 
                                  FontWeight="Bold" 
                                  Foreground="#DC2626"/>
                        <Border Background="#DC2626"
                               CornerRadius="12"
                               Padding="8,4"
                               Margin="12,0,0,0">
                            <TextBlock Text="{Binding Alerts.Count}" 
                                      FontSize="12" 
                                      FontWeight="Bold" 
                                      Foreground="White"/>
                        </Border>
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding Alerts}" MaxHeight="200">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White"
                                       CornerRadius="8"
                                       Padding="12"
                                       Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" 
                                                  FontSize="14" 
                                                  FontWeight="SemiBold" 
                                                  Foreground="#0F172A"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding Message}" 
                                                  FontSize="13" 
                                                  Foreground="#64748B"
                                                  TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- KPI CARDS (4 cartes en haut) -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Carte 1: Véhicules Totaux -->
                <Border Grid.Column="0" 
                       Background="#FFFFFF" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="12" 
                       Padding="20"
                       Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#3B82F6" Opacity="0.12" BlurRadius="12" ShadowDepth="4"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="BorderBrush" Value="#3B82F6"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Width="48" Height="48" Background="#EFF6FF" CornerRadius="10">
                                <TextBlock Text="🚗" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Véhicules" 
                                          FontSize="12" 
                                          Foreground="#64748B"/>
                                <TextBlock Text="{Binding TotalVehicles}" 
                                          FontSize="24" 
                                          FontWeight="Bold" 
                                          Foreground="#0F172A"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#EFF6FF" CornerRadius="6" Padding="6,4">
                            <TextBlock FontSize="12" Foreground="#3B82F6" FontWeight="Medium">
                                <Run Text="{Binding ActiveVehicles}"/>
                                <Run Text="actifs"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Carte 2: Carburant -->
                <Border Grid.Column="2" 
                       Background="#FFFFFF" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="12" 
                       Padding="20"
                       Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#10B981" Opacity="0.12" BlurRadius="12" ShadowDepth="4"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="BorderBrush" Value="#10B981"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Width="48" Height="48" Background="#F0FDF4" CornerRadius="10">
                                <TextBlock Text="⛽" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Pleins" 
                                          FontSize="12" 
                                          Foreground="#64748B"/>
                                <TextBlock Text="{Binding TotalFuelRecords}" 
                                          FontSize="24" 
                                          FontWeight="Bold" 
                                          Foreground="#0F172A"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#F0FDF4" CornerRadius="6" Padding="6,4">
                            <TextBlock FontSize="12" Foreground="#10B981" FontWeight="Medium">
                                <Run Text="{Binding MonthlyFuelCost, StringFormat={}{0:C0}}"/>
                                <Run Text="/mois"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Carte 3: Consommation -->
                <Border Grid.Column="4" 
                       Background="#FFFFFF" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="12" 
                       Padding="20"
                       Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#F59E0B" Opacity="0.12" BlurRadius="12" ShadowDepth="4"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="BorderBrush" Value="#F59E0B"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Width="48" Height="48" Background="#FEF3C7" CornerRadius="10">
                                <TextBlock Text="📊" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Consommation" 
                                          FontSize="12" 
                                          Foreground="#64748B"/>
                                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#0F172A">
                                    <Run Text="{Binding AverageFuelConsumption, StringFormat={}{0:F1}}"/>
                                    <Run Text="L/100km" FontSize="16"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#FEF3C7" CornerRadius="6" Padding="6,4">
                            <TextBlock FontSize="12" Foreground="#D97706" FontWeight="Medium">
                                <Run Text="Moyenne flotte"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Carte 4: Coût Total -->
                <Border Grid.Column="6" 
                       Background="#FFFFFF" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="12" 
                       Padding="20"
                       Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#EF4444" Opacity="0.12" BlurRadius="12" ShadowDepth="4"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="BorderBrush" Value="#EF4444"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Width="48" Height="48" Background="#FEE2E2" CornerRadius="10">
                                <TextBlock Text="💳" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Coût Total" 
                                          FontSize="12" 
                                          Foreground="#64748B"/>
                                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#0F172A">
                                    <Run Text="{Binding TotalFuelCost, StringFormat={}{0:N0}}"/>
                                    <Run Text="€" FontSize="16"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#FEE2E2" CornerRadius="6" Padding="6,4">
                            <TextBlock FontSize="12" Foreground="#DC2626" FontWeight="Medium">
                                <Run Text="{Binding TotalMaintenanceCost, StringFormat={}{0:N0}}"/>
                                <Run Text="€ maintenance"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- GRAPHIQUES ET VÉHICULES (Layout 2 colonnes) -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- COLONNE GAUCHE: Graphiques -->
                <StackPanel Grid.Column="0">
                    <!-- Graphique Consommation -->
                    <Border Background="#FFFFFF" 
                           BorderBrush="#E2E8F0" 
                           BorderThickness="1" 
                           CornerRadius="12" 
                           Padding="20" 
                           Margin="0,0,0,16">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="📈 Tendance de Consommation" 
                                      FontSize="16" 
                                      FontWeight="Bold" 
                                      Foreground="#0F172A" 
                                      Margin="0,0,0,16"/>
                            <lvc:CartesianChart Series="{Binding ConsumptionSeries}"
                                               Height="220"/>
                        </StackPanel>
                    </Border>

                    <!-- Graphique Coûts -->
                    <Border Background="#FFFFFF" 
                           BorderBrush="#E2E8F0" 
                           BorderThickness="1" 
                           CornerRadius="12" 
                           Padding="20">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="💰 Évolution des Coûts" 
                                      FontSize="16" 
                                      FontWeight="Bold" 
                                      Foreground="#0F172A" 
                                      Margin="0,0,0,16"/>
                            <lvc:CartesianChart Series="{Binding CostSeries}"
                                               Height="220"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- COLONNE DROITE: Top Véhicules -->
                <StackPanel Grid.Column="2">
                    <Border Background="#FFFFFF" 
                           BorderBrush="#E2E8F0" 
                           BorderThickness="1" 
                           CornerRadius="12" 
                           Padding="20">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🏆 Top Véhicules" 
                                      FontSize="16" 
                                      FontWeight="Bold" 
                                      Foreground="#0F172A" 
                                      Margin="0,0,0,16"/>
                            
                            <ItemsControl ItemsSource="{Binding TopVehiclesByCost}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F8FAFC"
                                               CornerRadius="8"
                                               Padding="12"
                                               Margin="0,0,0,8"
                                               Cursor="Hand">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <Border Grid.Column="0" 
                                                       Width="40" 
                                                       Height="40" 
                                                       Background="#EFF6FF" 
                                                       CornerRadius="8" 
                                                       Margin="0,0,12,0">
                                                    <TextBlock Text="🚗" 
                                                              FontSize="20" 
                                                              HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                                </Border>
                                                
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding RegistrationNumber}" 
                                                              FontSize="14" 
                                                              FontWeight="SemiBold" 
                                                              Foreground="#0F172A"/>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding TotalMileage, Mode=OneWay, StringFormat={}{0:N0}}" 
                                                                  FontSize="12" 
                                                                  Foreground="#64748B"/>
                                                        <TextBlock Text=" km" 
                                                                  FontSize="12" 
                                                                  Foreground="#64748B"/>
                                                    </StackPanel>
                                                </StackPanel>
                                                
                                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding TotalCost, Mode=OneWay, StringFormat={}{0:N0}}" 
                                                                  FontSize="14" 
                                                                  FontWeight="Bold" 
                                                                  Foreground="#3B82F6"/>
                                                        <TextBlock Text=" €" 
                                                                  FontSize="14" 
                                                                  FontWeight="Bold" 
                                                                  Foreground="#3B82F6"/>
                                                    </StackPanel>
                                                    <TextBlock Text="Coût total" 
                                                              FontSize="10" 
                                                              Foreground="#94A3B8"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- FILTRES ET VÉHICULES DISPONIBLES -->
            <StackPanel>
                <!-- En-tête Section -->
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="Véhicules Disponibles" 
                              FontFamily="Inter, Segoe UI"
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="#0F172A"/>
                    <Border HorizontalAlignment="Right"
                           Background="Transparent"
                           BorderBrush="#E2E8F0"
                           BorderThickness="1"
                           CornerRadius="8"
                           Padding="12,8"
                           Height="36"
                           Cursor="Hand">
                        <Button Background="Transparent"
                               BorderThickness="0"
                               Foreground="#475569"
                               FontSize="13"
                               Padding="0"
                               HorizontalContentAlignment="Center"
                               VerticalContentAlignment="Center"
                               Content="🔍 Filtrer"
                               Cursor="Hand"/>
                    </Border>
                </Grid>

                <!-- FILTRES COMPACTS -->
                <Border Background="#FFFFFF" 
                       BorderBrush="#E2E8F0" 
                       BorderThickness="1" 
                       CornerRadius="12" 
                       Padding="16" 
                       Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <WrapPanel Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,0" VerticalAlignment="Center">
                            <TextBlock Text="📍" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox SelectedIndex="0"
                                     Width="160"
                                     Height="36"
                                     FontSize="13"
                                     Background="#FFFFFF"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="1"
                                     Padding="10,0"
                                     VerticalContentAlignment="Center">
                                <ComboBoxItem Content="Toutes les localisations"/>
                                <ComboBoxItem Content="Paris, France"/>
                                <ComboBoxItem Content="Lyon, France"/>
                            </ComboBox>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,0" VerticalAlignment="Center">
                            <TextBlock Text="📅" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <DatePicker SelectedDate="{Binding StartDate}"
                                       Width="140" 
                                       Height="36"
                                       FontSize="13"
                                       VerticalContentAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,0" VerticalAlignment="Center">
                            <TextBlock Text="à" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#94A3B8"/>
                            <DatePicker SelectedDate="{Binding EndDate}"
                                       Width="140" 
                                       Height="36"
                                       FontSize="13"
                                       VerticalContentAlignment="Center"/>
                        </StackPanel>
                        
                        <Border Background="#F8FAFC"
                               BorderBrush="#E2E8F0"
                               BorderThickness="1"
                               CornerRadius="8"
                               Padding="10,8"
                               Height="36"
                               VerticalAlignment="Center"
                               Cursor="Hand">
                            <Button Background="Transparent"
                                   BorderThickness="0"
                                   Foreground="#475569"
                                   FontSize="13"
                                   Padding="0"
                                   Content="↺ Réinitialiser"
                                   Cursor="Hand"/>
                        </Border>
                    </WrapPanel>
                </Border>

                <!-- GRILLE DE CARTES VÉHICULES (3 colonnes) -->
                <ItemsControl ItemsSource="{Binding Vehicles}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FFFFFF" 
                                   BorderBrush="#E2E8F0" 
                                   BorderThickness="1" 
                                   CornerRadius="12" 
                                   Padding="0"
                                   Margin="0,0,12,12"
                                   Width="320"
                                   Cursor="Hand">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="8" ShadowDepth="2"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#3B82F6" Opacity="0.15" BlurRadius="16" ShadowDepth="6"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <!-- Image du véhicule -->
                                    <Border Height="160" 
                                           Background="#F1F5F9" 
                                           CornerRadius="12,12,0,0"
                                           Margin="0,0,0,0"
                                           ClipToBounds="True">
                                        <Grid>
                                            <Image Source="{Binding ImagePath, Converter={StaticResource VehicleImagePathConverter}}"
                                                  Stretch="UniformToFill"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Visibility="{Binding ImagePath, Converter={StaticResource NullToVisibilityConverter}}"/>
                                            <TextBlock Text="🚗" 
                                                      FontSize="72" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"
                                                      Opacity="0.4"
                                                      Visibility="{Binding ImagePath, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}"/>
                                            <!-- Bookmark icon -->
                                            <Border Width="32" 
                                                   Height="32" 
                                                   Background="#FFFFFF" 
                                                   CornerRadius="6" 
                                                   HorizontalAlignment="Right" 
                                                   VerticalAlignment="Top"
                                                   Margin="12,12,0,0"
                                                   Cursor="Hand">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="4" ShadowDepth="2"/>
                                                </Border.Effect>
                                                <TextBlock Text="🔖" 
                                                          FontSize="16" 
                                                          HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- Détails du véhicule -->
                                    <StackPanel Margin="16">
                                        <TextBlock Text="{Binding DisplayName}" 
                                                  FontSize="15" 
                                                  FontWeight="SemiBold" 
                                                  Foreground="#0F172A" 
                                                  Margin="0,0,0,10"/>
                                        
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Type" 
                                                          FontSize="11" 
                                                          Foreground="#94A3B8" 
                                                          Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding VehicleType}" 
                                                          FontSize="13" 
                                                          FontWeight="Medium" 
                                                          Foreground="#0F172A"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="Carburant" 
                                                          FontSize="11" 
                                                          Foreground="#94A3B8" 
                                                          Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding FuelType}" 
                                                          FontSize="13" 
                                                          FontWeight="Medium" 
                                                          Foreground="#0F172A"/>
                                            </StackPanel>
                                        </Grid>

                                                        <Grid Margin="0,4,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <Border Background="#EFF6FF" 
                                                   BorderBrush="#3B82F6" 
                                                   BorderThickness="1" 
                                                   CornerRadius="6" 
                                                   Padding="8,6"
                                                   HorizontalAlignment="Stretch">
                                                <TextBlock Text="{Binding Status}" 
                                                          FontSize="12" 
                                                          FontWeight="SemiBold" 
                                                          Foreground="#3B82F6"
                                                          HorizontalAlignment="Center"/>
                                            </Border>
                                            
                                            <TextBlock Grid.Column="1" 
                                                      FontSize="15" 
                                                      FontWeight="Bold" 
                                                      Foreground="#3B82F6"
                                                      VerticalAlignment="Center"
                                                      Margin="12,0,0,0">
                                                <Run Text="{Binding PurchasePrice, StringFormat={}{0:N0}}"/>
                                                <Run Text="€"/>
                                            </TextBlock>
                                        </Grid>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
