using System.Windows;

namespace FleetManager.Views
{
    /// <summary>
    /// Dashboard moderne démontrant le nouveau système de design
    /// </summary>
    public partial class ModernDashboard : Window
    {
        public ModernDashboard()
        {
            InitializeComponent();
            
            // Exemple: Charger les données de démonstration
            // DataContext = new DashboardViewModel();
        }
    }
}
